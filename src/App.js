import React from 'react';
import logo from './logo.svg';
import './App.css';
import {Form} from './Form'

function App() {
  return (
    <div className="App">
      <Form>
        
      </Form>
      {/* form component
        //state num questions
      input components
        //input state is true or false
        // onchange calculateInput state passed from form component
      progress bar component
        //takes fraction completed as a styling prop */}
    </div>
  );
}

export default App;
